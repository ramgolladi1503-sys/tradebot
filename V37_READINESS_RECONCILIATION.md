# V37 readiness reconciliation

The frozen 118-node prospective and 47-node A-X authorities are green on the
current successor. Storage bounds are implemented for the verified core paths.
Final readiness is PASS: the exact-SHA internal release image is materialized,
its `SOURCE_SHA` matches the committed successor, and both frozen authorities
pass from that image.
