[pytest]
testpaths = tests
norecursedirs = data logs
addopts = -m "not integration"
markers =
    unit: deterministic unit test (default gate)
    integration: requires network/secrets/external dependencies; excluded by default
