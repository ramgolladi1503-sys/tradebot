[pytest]
pythonpath = .
addopts = -q
filterwarnings =
    ignore:jsonschema.RefResolver is deprecated.*:DeprecationWarning:altair.utils.schemapi
markers =
    unit: deterministic unit test
    behavior: intended product behavior
    safety: fail-closed safety boundary
    regression: known-defect regression
