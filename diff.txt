diff --git a/config/config.py b/config/config.py
index 31b53dbb..c49f3859 100644
--- a/config/config.py
+++ b/config/config.py
@@ -2800,7 +2800,7 @@ _opt_list = [s.strip() for s in _optional_default.split(",") if s.strip()]
 
 CROSS_FEED_STATUS = {}
 
-def _set_feed_status(feed_key: str, status: str, reason: str | None = None):
+def _set_feed_status(feed_key: str, status: str, reason=None):
     CROSS_FEED_STATUS[feed_key] = {"status": status, "reason": reason}
 
 def _classify_feed(feed_key: str, preferred: str):
diff --git a/core/feed_truth_contract.py b/core/feed_truth_contract.py
index 117472f8..73e78eb5 100644
--- a/core/feed_truth_contract.py
+++ b/core/feed_truth_contract.py
@@ -14,6 +14,8 @@ RECOVERY_BLOCKED = "RECOVERY_BLOCKED"
 AUTH_BLOCKED = "AUTH_BLOCKED"
 IMPORT_MISSING = "IMPORT_MISSING"
 UNKNOWN = "UNKNOWN"
+RECONNECTING = "RECONNECTING"
+FAILED = "FAILED"
 
 _KNOWN_STATES = {
     LIVE,
@@ -24,13 +26,17 @@ _KNOWN_STATES = {
     AUTH_BLOCKED,
     IMPORT_MISSING,
     UNKNOWN,
+    RECONNECTING,
+    FAILED,
 }
 
 _STATE_PRIORITY = (
     AUTH_BLOCKED,
     IMPORT_MISSING,
     RECOVERY_BLOCKED,
+    FAILED,
     DISCONNECTED,
+    RECONNECTING,
     STALE,
     DEGRADED,
     LIVE,
@@ -248,6 +254,10 @@ def build_feed_truth_contract(snapshot: Mapping[str, Any] | None = None) -> Feed
         blockers.append(IMPORT_MISSING)
     if process_restart_required:
         blockers.append(RECOVERY_BLOCKED)
+    if runtime_state == RECONNECTING:
+        blockers.append(RECONNECTING)
+    if runtime_state == FAILED:
+        blockers.append(FAILED)
     if _is_disconnected(source):
         blockers.append(DISCONNECTED)
 
@@ -293,8 +303,12 @@ def build_feed_truth_contract(snapshot: Mapping[str, Any] | None = None) -> Feed
         state = IMPORT_MISSING
     elif RECOVERY_BLOCKED in deduped_blockers:
         state = RECOVERY_BLOCKED
+    elif FAILED in deduped_blockers:
+        state = FAILED
     elif DISCONNECTED in deduped_blockers:
         state = DISCONNECTED
+    elif RECONNECTING in deduped_blockers:
+        state = RECONNECTING
     elif STALE in deduped_blockers:
         state = STALE
     elif DEGRADED in deduped_blockers or advisory_reasons:
diff --git a/core/kite_depth_ws.py b/core/kite_depth_ws.py
index aa588090..ae95ee20 100644
--- a/core/kite_depth_ws.py
+++ b/core/kite_depth_ws.py
@@ -3368,17 +3368,31 @@ def _run_db_tick_watchdog_cycle(
                 "stale_strikes": 0,
             },
         )
-    elif db_tick_age_sec is not None and db_tick_age_sec > float(stale_restart_sec):
+    ws_stale_limit = float(getattr(cfg, "MAX_DEPTH_AGE_SEC", 5.0))
+    is_stale = False
+    stale_source = "unknown"
+    stale_age = 0.0
+
+    if db_tick_age_sec is not None and db_tick_age_sec > float(stale_restart_sec):
+        is_stale = True
+        stale_source = "db"
+        stale_age = db_tick_age_sec
+    elif ws_tick_age_sec is not None and ws_tick_age_sec > float(ws_stale_limit):
+        is_stale = True
+        stale_source = "ws"
+        stale_age = ws_tick_age_sec
+
+    if is_stale:
         _STALE_STRIKES += 1
         _log_ws(
             "FEED_TICK_STALE",
-            {"age_sec": db_tick_age_sec, "source": "db", "strikes": _STALE_STRIKES},
+            {"age_sec": stale_age, "source": stale_source, "strikes": _STALE_STRIKES},
             throttle_key="FEED_TICK_STALE",
         )
         _emit_feed_health(
             "FEED_STALE",
             {
-                "reason": "db_tick_stale",
+                "reason": f"{stale_source}_tick_stale",
                 "last_ws_tick_epoch": ws_tick_epoch,
                 "last_ws_tick_age_sec": ws_tick_age_sec,
                 "last_db_tick_epoch": db_tick_epoch,
@@ -3406,7 +3420,7 @@ def _run_db_tick_watchdog_cycle(
                     )
                 except TypeError:
                     restarted = bool(cb("tick_stalled"))
-    elif db_tick_age_sec is not None and db_tick_age_sec <= float(reset_sec):
+    elif not is_stale and ((db_tick_age_sec is not None and db_tick_age_sec <= float(reset_sec)) or (ws_tick_age_sec is not None and ws_tick_age_sec <= float(reset_sec))):
         if _STALE_STRIKES:
             _log_ws("FEED_TICK_RECOVERED", {"age_sec": db_tick_age_sec, "source": "db", "strikes": _STALE_STRIKES})
         _STALE_STRIKES = 0
@@ -5554,17 +5568,23 @@ def start_depth_ws(instrument_tokens, profile_verified=False, skip_lock: bool =
                 runtime_state="RUNNING",
                 last_error="",
             )
-            _log_ws("FEED_RECONNECT", {"attempts": attempts})
         except Exception as exc:
+            _log_ws("FEED_RECONNECT_RESUBSCRIBE_ERROR", {"attempts": attempts, "error": str(exc)})
             _RUNTIME_STATE = "SUBSCRIBE_FAILED"
-            _LAST_RUNTIME_ERROR = str(exc)
-            _log_ws("FEED_RECONNECT_ERROR", {"error": str(exc), "attempts": attempts})
-            _persist_runtime_snapshot_row(
-                ws_connected=False,
-                source=f"on_reconnect:{attempts}:error",
-                runtime_state="SUBSCRIBE_FAILED",
-                last_error=_LAST_RUNTIME_ERROR,
-            )
+            _LAST_RUNTIME_ERROR = f"reconnect_resubscribe_error:{exc}"[:1000]
+
+    def on_noreconnect(ws):
+        global _RUNTIME_STATE, _LAST_RUNTIME_ERROR
+        _log_ws("FEED_NORECONNECT", {"ws_lifecycle_state": "DISCONNECTED"})
+        _RUNTIME_STATE = "FAILED"
+        _LAST_RUNTIME_ERROR = "native_reconnect_failed"
+        _persist_runtime_snapshot_row(
+            ws_connected=False,
+            source="on_noreconnect",
+            runtime_state="FAILED",
+            last_error=_LAST_RUNTIME_ERROR,
+        )
+
 
     ws_fault_seen = False
 
@@ -5637,36 +5657,32 @@ def start_depth_ws(instrument_tokens, profile_verified=False, skip_lock: bool =
             if _should_require_process_restart_for_ws_fault(code=code_int, reason_text=reason_text):
                 _block_reconnect_for_process_restart(source="on_error", code=code_int, reason=reason_text, ticker=ws)
                 return
-            if _use_internal_reconnect():
-                if _reconnect_recovery_blocked_active():
-                    blocked_reason = str(_RECONNECT_BLOCKED_REASON).strip().lower()
-                    _emit_reconnect_recovery_blocked_snapshot(
-                        source="on_error:reconnect_suppressed",
-                        reason=blocked_reason,
-                    )
-                    return
-                _persist_runtime_snapshot_row(
-                    ws_connected=False,
-                    source=f"on_error:restart_attempt:{code}",
-                    runtime_state="RESTARTING",
-                    last_error=_LAST_RUNTIME_ERROR,
-                    disconnected_code=code_int,
-                    disconnected_reason=reason_text,
-                    restart_attempt_allowed=True,
-                    restart_attempted=True,
-                )
-                _log_ws(
-                    "FEED_INTERNAL_RECONNECT_FORCE_FULL",
-                    {"source": "on_error", "code": code, "reason": reason_text},
-                )
-                _schedule_restart_depth_ws(
-                    reason=f"ws_error:{code}",
-                    ignore_cooldown=ignore_cooldown,
-                    force_full_restart=True,
-                    source="on_error",
+        if _use_internal_reconnect():
+            if _reconnect_recovery_blocked_active():
+                blocked_reason = str(_RECONNECT_BLOCKED_REASON).strip().lower()
+                _emit_reconnect_recovery_blocked_snapshot(
+                    source="on_error:reconnect_suppressed",
+                    reason=blocked_reason,
                 )
                 return
-            restart_depth_ws(reason=f"ws_error:{code}", ignore_cooldown=ignore_cooldown)
+            _RUNTIME_STATE = "RECONNECTING"
+            _LAST_RUNTIME_ERROR = f"reconnecting_after_error_{code}"
+            _persist_runtime_snapshot_row(
+                ws_connected=False,
+                source=f"on_error:reconnecting:{code}",
+                runtime_state="RECONNECTING",
+                last_error=_LAST_RUNTIME_ERROR,
+                disconnected_code=code_int,
+                disconnected_reason=reason_text,
+                restart_attempt_allowed=True,
+                restart_attempted=True,
+            )
+            _log_ws("FEED_INTERNAL_RECONNECT_WAIT", {"source": "on_error", "code": code, "reason": reason_text})
+            # Let the native reconnect in KiteTicker handle this.
+            return
+        
+        # Native reconnect handles it, but if internal is disabled, we fallback to our manual restart logic.
+        restart_depth_ws(reason=f"ws_error:{code}", ignore_cooldown=ignore_cooldown)
 
     def on_close(ws, code, reason):
         nonlocal ws_fault_seen
@@ -5775,12 +5791,26 @@ def start_depth_ws(instrument_tokens, profile_verified=False, skip_lock: bool =
                         source="on_close",
                     )
                     return
+                
+                _RUNTIME_STATE = "RECONNECTING"
+                _LAST_RUNTIME_ERROR = f"reconnecting_after_close_{code}"
+                _persist_runtime_snapshot_row(
+                    ws_connected=False,
+                    source=f"on_close:reconnecting:{code}",
+                    runtime_state="RECONNECTING",
+                    last_error=_LAST_RUNTIME_ERROR,
+                    disconnected_code=code_int,
+                    disconnected_reason=reason_text,
+                    restart_attempt_allowed=True,
+                    restart_attempted=True,
+                )
                 _log_ws(
                     "FEED_INTERNAL_RECONNECT_WAIT",
                     {"source": "on_close", "code": code, "reason": reason_text},
                 )
                 _log_ws("ws_reconnect_attempt", {"attempts": 1, "source": "on_close", "ws_lifecycle_state": "RECONNECTING"})
-                _soft_resubscribe_current(reason=f"on_close:{code}")
+                # We do NOT manually try to _soft_resubscribe_current here; kiteconnect's native reconnect
+                # will call on_reconnect or on_connect when it succeeds. We just wait.
                 return
             restart_depth_ws(reason=f"ws_close:{code}", ignore_cooldown=ignore_cooldown)
 
@@ -5831,10 +5861,12 @@ def start_depth_ws(instrument_tokens, profile_verified=False, skip_lock: bool =
                 state_machine = {"state": "DOWN", "reason": "ws_disconnected"}
             elif last_tick_age_sec is None:
                 state_machine = {"state": "STARTING", "reason": "awaiting_first_tick"}
-            elif last_tick_age_sec <= 10.0:
+            elif last_tick_age_sec <= float(getattr(cfg, "MAX_QUOTE_AGE_SEC", 2.0)):
                 state_machine = {"state": "LIVE", "reason": "ticks_flowing"}
+            elif last_tick_age_sec <= float(getattr(cfg, "MAX_DEPTH_AGE_SEC", 5.0)):
+                state_machine = {"state": "DEGRADED", "reason": "ticks_delayed"}
             else:
-                state_machine = {"state": "DOWN", "reason": "no_ws_messages"}
+                state_machine = {"state": "STALE", "reason": "no_ws_messages"}
             option_state = _option_runtime_state(
                 now_epoch=now_epoch,
                 tokens=_LAST_TOKENS,
@@ -6273,6 +6305,7 @@ def start_depth_ws(instrument_tokens, profile_verified=False, skip_lock: bool =
 
     kws.on_connect = on_connect
     kws.on_reconnect = on_reconnect
+    kws.on_noreconnect = on_noreconnect
     kws.on_error = on_error
     kws.on_close = on_close
     kws.on_ticks = on_ticks
diff --git a/core/orchestrator.py b/core/orchestrator.py
index b977ce9c..dc3e630d 100644
--- a/core/orchestrator.py
+++ b/core/orchestrator.py
@@ -5231,6 +5231,34 @@ class Orchestrator:
                         or ((market_data.get("market_context") or {}).get("execution_mode") if isinstance(market_data.get("market_context"), dict) else "")
                         or getattr(cfg, "EXECUTION_MODE", "")
                     ).strip().upper()
+                    
+                    try:
+                        feed_ok, feed_reasons = self._pilot_feed_ok()
+                    except Exception:
+                        feed_ok, feed_reasons = False, ["feed_unhealthy"]
+
+                    if feed_ok is False:
+                        cycle_candidates_blocked += 1
+                        cycle_blockers["feed_unhealthy"] += 1
+                        logger.warning(
+                            "feed_unhealthy_prebuild_skip symbol=%s execution_mode=%s reasons=%s",
+                            sym,
+                            execution_mode,
+                            ",".join(feed_reasons or [])
+                        )
+                        try:
+                            self._log_decision_safe(
+                                self._build_decision_event(
+                                    None,
+                                    market_data,
+                                    gatekeeper_allowed=False,
+                                    veto_reasons=["feed_unhealthy"],
+                                )
+                            )
+                        except Exception:
+                            pass
+                        continue
+
                     if _should_skip_trade_builder_for_latency_guard(
                         latency_soften_active=latency_soften_active,
                         execution_mode=execution_mode,
