# Independent Verification Specification

These JSON files are data-only authority for the standard-library verifier. They define the semantic allowlist, explicit non-semantic denylist, canonical encoding, instrument categories, authority flags, and campaign binding. The verifier must reject unknown semantic fields and all mismatches before any feed boundary.
