# V38 live evidence-root contract

Future root template:
`/Volumes/TradeBotData/live-verification-73eb9e1d-<YYYYMMDD>-<session_id>/`

Required directories: `auth`, `feed`, `ticks`, `depth`, `db`, `snapshots`,
`cas`, `funnel`, `risk`, `storage`, `failover`, `logs`, `seal`, and
`postclose`. V38 creates no market evidence and performs no broker activity.
