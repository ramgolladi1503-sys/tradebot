# Runtime Path Map

market feed -> NIFTY constituent universe -> completed constituent bars -> completed_constituent_breadth_snapshots -> market_event_graph_history -> market_event_graph_reversal_v1 -> advisory candidate path.

Current campaign harness consumes captured metadata JSONL and does not fetch data or call brokers.
