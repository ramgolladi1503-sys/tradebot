# Pre-outcome freeze

The feature set, representative-contract rule, depth, minimum leaf size, leaf gates, signal cap, cooldown, friction, outer folds, inner blocks, permutation control and holdout gates are frozen before result inspection. Any later modification requires a new campaign version and counts toward multiplicity.
