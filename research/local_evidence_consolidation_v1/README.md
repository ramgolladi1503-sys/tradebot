# Local Evidence Consolidation V1

This directory preserves local TradeBot research evidence copied from isolated worktrees and local evidence folders. It is evidence-only: no production runtime code, broker calls, provider calls, or live trading changes are included.

See `CONSOLIDATION_MANIFEST.json` for source buckets, file counts, sizes, and SHA-256 hashes.
