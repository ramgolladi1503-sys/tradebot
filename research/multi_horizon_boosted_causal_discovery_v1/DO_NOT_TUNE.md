# Pre-outcome freeze

The causal feature set, representative-contract rule, candidate horizons, confidence quantiles, model configuration, chronological discovery/calibration split, signal cap, cooldown, friction, permutation control, outer folds and holdout gates are frozen before result inspection. Any later change requires a new campaign version and counts toward multiplicity.
