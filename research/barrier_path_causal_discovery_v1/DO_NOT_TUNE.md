# Pre-outcome freeze

Target, stop, maximum hold, same-candle ambiguity, representative-contract selection, causal feature set, model depth, leaf gates, inner blocks, outer folds, signal cap, cooldown, friction, permutation control and holdout gates are frozen before result inspection. Any later change requires a new campaign version and counts toward multiplicity.
