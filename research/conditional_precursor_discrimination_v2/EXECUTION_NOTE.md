# Execution Path

This campaign is executed through GitHub Actions with `actions/checkout` configured for Git LFS. Evidence artifacts are uploaded from every run, including failing runs, so the first divergence can be inspected without relying on completion summaries.
