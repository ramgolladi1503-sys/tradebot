# V37 independent storage model verifier

The verifier requirements are reproducible from the successor: recompute the
protocol maxima, queue products, batch sum, JSONL limits, atomic-artifact cap,
and SQLite PRAGMAs from source and tests. Current evidence is the V37 focused
suite plus the exact 118-node and A-X runs. A release is invalid if any value
differs from the register.
