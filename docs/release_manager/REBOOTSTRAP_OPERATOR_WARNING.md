# Operator warning

Do not run `rebootstrap-promote` against the canonical release store from this branch yet. The implementation has not been executed in a repository runtime by the authoring connector, and the 22-case behavioral mutation campaign intentionally remains BLOCKED.

Use a copied release store for first execution validation.
