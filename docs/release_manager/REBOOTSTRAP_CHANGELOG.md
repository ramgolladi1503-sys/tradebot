# Rebootstrap V1 changes

- schema-v2 append-only `GOVERNED_REBOOTSTRAP` release-store event;
- no trusted fallback to quarantined history;
- full 18-gate rebootstrap certification contract;
- independent rebootstrap verifier and candidate/predecessor/dependency binding;
- explicit certify/verify/promote CLI commands;
- fail-closed behavioral tests;
- 22-attack registry and intentionally BLOCKED mutation runner pending behavioral mutators;
- normal release verifier binding corrected to include dependency-graph digest, matching promotion.
