# PR scope

Allowed: release-store schema/read integrity, exceptional quarantine recovery certification, independent release verification, release-manager CLI, recovery tests/docs/adversarial campaign.

Prohibited: strategy logic, candidate selection, ranking semantics, risk rules, broker integration, market feed behavior, websocket behavior, ExecutionRouter, order authority, paper/live authorization, edge claims.

The branch must remain fail-closed until executable validation is complete.
