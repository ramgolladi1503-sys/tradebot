# Non-main validator review note

This directory intentionally does not carry a self-referential exact-candidate manifest for the repair branch. The frozen-head bridge validator retains its existing fallback to the inherited base-authoritative review manifest on `origin/main`. Candidate-specific review evidence for this repair is recorded in `docs/agent_reviews/frozen_head_validator_non_main_base_v1.md`.
