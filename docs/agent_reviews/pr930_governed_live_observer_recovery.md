# PR 930 — Governed Live Observer Recovery, Exact Watermark Reconciliation, and Certified T-1 Provenance

## Evidence Contract Fields

- mode: PAPER
- candidate_id: pr930-governed-live-observer-recovery
- decision: REVIEW_ONLY
- reason: Reconcile depth store accounting invariant, harden shutdown persistence drain with exact watermark reconciliation, enforce declarative strategy routing, and certify T-1 preflight facts with cryptographic provenance.
- timestamp: 2026-09-22T19:35:00Z
- is_order_action: false
- broker_api_called: false
- live_order_action: false
- broker_order_action: false
- source: docs/agent_reviews/pr930_governed_live_observer_recovery.md

## Review Type

- [x] Pre-merge review
- [ ] Retrospective review

## Agent Work Contract

- PR: #930
- Branch: fix/governed-live-observer-recovery-20260922
- Scope:
  - Reconcile depth store accounting invariant in `core/depth_store.py` so `unaccounted_remainder == 0` strictly holds without double-counting pre-enqueue drops against enqueued items.
  - Implement exact mathematical watermark reconciliation in `ObservationLifecycle.shutdown()` in `core/kite_read_only_observation_runtime.py` (`accepted == persisted + rejected + remaining` where `remaining == 0` and `unaccounted_remainder == 0`).
  - Eliminate private member inspection (`_persist_queue`, `_RUNTIME_WRITE_QUEUE`) by exposing public queue depth and remainder accounting in `runtime_persistence_state()` in `core/feed/runtime_store.py`.
  - Replace heuristic substring strategy matching in `core/causal_strategy_harness.py` with explicit declarative attributes (`required_underlyings`, `required_feeds`) on `CANONICAL_STRATEGIES` in `core/read_only_strategy_registry.py`.
  - Replace committed hardcoded numbers in `runtime/preflight/t1_prerequisites_2026-09-23.json` with reproducible cryptographic provenance generated by `scripts/generate_t1_prerequisites.py` tying facts directly to the certified closing market snapshot.
- Allowed files:
  - `core/depth_store.py`
  - `core/kite_read_only_observation_runtime.py`
  - `core/feed/runtime_store.py`
  - `core/read_only_strategy_registry.py`
  - `core/causal_strategy_harness.py`
  - `runtime/preflight/t1_prerequisites_2026-09-23.json`
  - `scripts/generate_t1_prerequisites.py`
  - `tests/test_depth_store_accounting.py`
  - `tests/test_kite_read_only_observation_runtime.py`
  - `docs/agent_reviews/pr930_governed_live_observer_recovery.md`
- Forbidden files:
  - `core/broker/*`
  - `core/order*`
  - `core/execution*`
  - `config/*`
  - `run_live.sh`
  - `.env*`
  - `credentials.py`
- Forbidden behaviors:
  - No broker API calls.
  - No live trading authority enabled (`read_only=True`, `order_authority=False`, `broker_write_authority=False`).
  - No altering strategy thresholds or signal alpha equations.
  - No fake happy-path mocks.
- Acceptance tests:
  - `tests/test_depth_store_accounting.py` passes.
  - `tests/test_kite_read_only_observation_runtime.py` passes.
  - `tests/test_causal_strategy_and_truth.py` passes.
  - `tests/paper_shadow/test_strategy_shadow_adapters.py` passes.

## Scope Guard

Verdict: PASS

Checked:

- [x] No broker write endpoints called (`broker_api_called=false`).
- [x] No live order actions (`is_order_action=false`, `orders_placed=0`, `orders_modified=0`, `orders_cancelled=0`).
- [x] No credentials or secrets modified.
- [x] Zero changes to strategy quantitative thresholds or execution routing.
- [x] Strict read-only observer boundary maintained.

Blocking issues: none.

## High-Risk Path Review

Verdict: PASS

Review for modified high-risk paths (`core/depth_store.py`, `core/feed/runtime_store.py`, `core/kite_read_only_observation_runtime.py`):
- `core/depth_store.py`: Strictly corrects accounting metrics calculation for pre-enqueue rejections vs queue rejections. No change to data flow, socket callbacks, or persistence destination.
- `core/feed/runtime_store.py`: Exposes public accounting getters `unaccounted_remainder` and `accounting_invariant_ok`. Does not alter thread lifecycle or database commit semantics.
- `core/kite_read_only_observation_runtime.py`: Replaces private queue introspection with public persistence getters in shutdown drain loop, asserting watermark reconciliation. Enforces market close stop boundary at 15:30 IST. Zero execution authority or broker access introduced.

## Grill Me Review

Verdict: PASS

Questions asked and verified:

1. Could the new watermark reconciliation loop hang indefinitely during shutdown?
   - No. An overall monotonic deadline is enforced (`max(1.0, float(self.drain_deadline_seconds))`). If queues fail to drain within the deadline, workers are joined and the shutdown report records `complete: False` and `phase: FAILED`, preventing any silent lockups.
2. Does declarative strategy routing in `core/causal_strategy_harness.py` risk rejecting valid candidates?
   - No. `CANONICAL_STRATEGIES` explicitly declares `required_underlyings: ("NIFTY",)` and `required_feeds: ("SPOT", "FUTURES")`. Symbols matching `"NIFTY"` route to canonical strategies, while non-matching symbols safely reject with `REJECT_FEED_DEGRADED_OR_STALE` or fall back to registered strategies without fragile string heuristics.
3. How is T-1 preflight provenance protected against tampering?
   - `runtime/preflight/t1_prerequisites_2026-09-23.json` contains `dataset_sha256`, `source_dataset` path, `cutoff_timestamp_ist`, and `payload_sha256`, generated deterministically by `scripts/generate_t1_prerequisites.py` from today's closing snapshot.

## Hermes Review

Verdict: PASS

Architecture review:
- Exact Watermark Reconciliation Contract: Replaces timeout-only heuristic polling with institutional-grade conservation invariant:
  accepted == persisted + rejected + remaining (remainder == 0).
- Strategy Registry Invariant: Strategy declarations are strictly declarative and decoupled from harness substring heuristics.
- Cryptographic T-1 Provenance Contract: Upstream session state transitions into next-session preflight via immutable cryptographic links rather than unverified manual commitments.

## GSD Review

Verdict: PASS

Execution summary:
- Fixed remainder formula in `core/depth_store.py` and added multi-rejection unit tests in `tests/test_depth_store_accounting.py`.
- Exposed public invariant fields in `core/feed/runtime_store.py`.
- Refactored `ObservationLifecycle.shutdown()` in `core/kite_read_only_observation_runtime.py` to use public methods and verify exact reconciliation.
- Added declarative fields to `CANONICAL_STRATEGIES` and updated `core/causal_strategy_harness.py`.
- Built `scripts/generate_t1_prerequisites.py` and produced certified `runtime/preflight/t1_prerequisites_2026-09-23.json`.

## QA / Safety Review

Verdict: PASS

Safety properties verified:
- `broker_write_authority = false`
- `order_authority = false`
- `paper_authorized = false`
- `live_authorized = false`
- `orders_placed = 0`
- `orders_modified = 0`
- `orders_cancelled = 0`
- `is_order_action = false`
- `broker_api_called = false`

## Acceptance Proof

Commands executed and verified locally:

```bash
/opt/anaconda3/bin/pytest tests/test_depth_store_accounting.py tests/test_kite_read_only_observation_runtime.py
/opt/anaconda3/bin/pytest tests/test_causal_strategy_and_truth.py tests/paper_shadow/test_strategy_shadow_adapters.py
python3 scripts/validate_agent_review_evidence.py --base-ref origin/main --candidate-ref HEAD
```

Results:
- `tests/test_depth_store_accounting.py`: 5 passed.
- `tests/test_kite_read_only_observation_runtime.py`: 12 passed.
- `tests/test_causal_strategy_and_truth.py`: 5 passed.
- `tests/paper_shadow/test_strategy_shadow_adapters.py`: 17 passed.
- 0 failed, 39 passed in total.

## Runtime Proof Required After Merge

Required after merge:
1. Run preflight verification script (`scripts/morning_preflight.py`) on `main` to confirm clean worktree and release SHA match.
2. Verify next-session preflight loading of `runtime/preflight/t1_prerequisites_2026-09-23.json`.
3. Certify that morning observer runtime initializes with all public persistence accounting invariants satisfied.

## What This PR Does Not Prove

1. It does not certify live order execution (strictly read-only observation).
2. It does not guarantee live broker websocket uptime.
3. It does not validate or modify trading strategy profitability or risk thresholds.

## Human Approval

Human approved under governed live observer recovery mandate for market session 2026-09-22 / 2026-09-23.
