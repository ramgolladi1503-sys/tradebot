# TradeBot Antigravity MCP v1 — Agent Review Evidence

- mode: RESEARCH_TOOLING
- candidate_id: tradebot-antigravity-mcp-v1
- decision: ADD_READ_ONLY_MCP_EVIDENCE_AND_AUDIT_LAYER
- reason: Replace narrative-only Antigravity completion claims with structured read-only evidence, source-audit, gate, and Git-scope tools.
- timestamp: 2026-07-22T06:30:00Z
- is_order_action: false
- broker_api_called: false
- live_order_action: false
- broker_order_action: false
- source: docs/agent_reviews/tradebot-antigravity-mcp-v1.md

## Agent Work Contract

- Branch: `feature/tradebot-antigravity-mcp-v1`
- Scope: add read-only MCP evidence, data-audit, machine-gate, and Git-audit services for Antigravity.
- Allowed paths: `tools/tradebot_mcp/`, `tests/mcp/`, `.agents/`, `.github/workflows/tradebot-mcp.yml`, `docs/antigravity/`, this evidence file, `requirements-mcp.txt`, `scripts/install_antigravity_mcp.sh`, and `.gitignore`.
- Forbidden behavior: no broker calls, order placement, production strategy changes, live execution, feed changes, risk changes, dashboard changes, credentials, arbitrary shell execution, or Git write operations.

## Scope Guard

Verdict: PASS

The PR adds developer tooling, isolated CI, and documentation only. Production strategy, broker, execution, risk, feed, ranking, dashboard, credentials, and live configuration paths are unchanged.

Blocking issues: none.

## Grill Me Review

Verdict: PASS

Questions checked:

1. Can an MCP tool escape the approved roots? The path policy resolves symlinks and rejects paths outside configured roots.
2. Can it expose secrets? Common environment, credential, private-key, and broker-token paths are blocked.
3. Can it execute arbitrary commands? No. The gate service only validates evidence manifests, and Git commands are fixed read-only operations.
4. Can an agent manufacture a gate pass by editing a status string? No. Every check requires a zero exit code, producer commit, artifact path, and matching SHA-256.

## Hermes Review

Verdict: PASS

Architecture review:

- Core services are independent of the MCP transport layer.
- Four separate stdio servers permit separate Antigravity permission policies.
- The MCP dependency set is isolated from the production requirements file.
- Local absolute paths are generated by an installer into an ignored workspace config.
- A dedicated workflow validates the isolated dependency set, server imports, and installer output.

## GSD Review

Verdict: PASS

Execution sequence:

1. Install the isolated MCP environment.
2. Generate `.agents/mcp_config.json`.
3. Reload MCP servers in Antigravity.
4. Keep permissions in Ask mode initially.
5. Use evidence and Git tools before trusting subagent handoffs.
6. Use machine gates before declaring any research milestone complete.

## QA / Safety Review

Required tests:

```bash
pytest -q tests/mcp/test_core.py
python -m pip install -r requirements-mcp.txt
pytest -q -m integration tests/mcp/test_servers.py
```

Safety coverage includes path traversal, symlink escape, secret-path rejection, Parquet temporal checks, artifact-hash mutation, required-gate checks, Git scope verification, and Git option-injection rejection.

## High-Risk Path Review

Verdict: NOT APPLICABLE

No repository high-risk runtime path is modified. The new GitHub Actions workflow is limited to MCP paths and has read-only contents permission.

## Acceptance Proof

Commands:

```bash
pytest -q tests/mcp/test_core.py
bash scripts/install_antigravity_mcp.sh
pytest -q -m integration tests/mcp/test_servers.py
```

Expected results:

- focused core tests pass;
- installer creates the ignored `.agents/mcp_config.json` and an isolated `.venv-mcp`;
- all four MCP server modules import successfully;
- no production file is changed.

## Runtime Proof Required After Merge

In an isolated local checkout:

```bash
bash scripts/install_antigravity_mcp.sh
```

Then reload Antigravity MCP servers and invoke:

- `tradebot-git-audit.get_worktree_status`;
- `tradebot-evidence.list_research_contexts`;
- `tradebot-data-audit.list_corpora`;
- a deliberately incomplete `tradebot-gates.run_wave1_gate` manifest, which must return `FAIL`.

## What This PR Does Not Prove

1. It does not prove a structural trading edge.
2. It does not prove option profitability.
3. It does not certify any existing research source or pipeline automatically.
4. It does not make Antigravity reasoning infallible.
5. It does not grant production, broker, or order authority.
6. It does not replace independent review of market-data semantics and gate artifacts.

## Human Approval

Approved by: repository owner through the instruction to repair, validate, and merge valuable infrastructure/runtime PRs.

Date: 2026-07-29.

Merge remains conditional on current required checks passing and GitHub branch protection allowing the operation.
