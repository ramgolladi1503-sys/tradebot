mode: RESEARCH_ONLY_AUTHORITY_CLOSURE
candidate_id: all_strategy_authority_closure_v1
decision: INVALID_IMPLEMENTATION
reason: Commit 2d7dfa1eaab7afa9b60e37967cd6236b0edcb152 reads real full census registries in code, but it does not publish regenerated registry-derived authority evidence, hardcodes Mac-only input paths in production and pytest, and still leaves the previously fabricated authority JSON files in the repository.
timestamp: 2026-07-24T16:15:00+05:30
research_only: true
read_only: true
is_order_action: false
broker_api_called: false
allowed_for_live_execution: false
source: Independent GitHub audit of commit 2d7dfa1eaab7afa9b60e37967cd6236b0edcb152 and the committed authority-closure artifacts.

# All-Strategy Authority Closure v1 — Second Invalidation

## Agent Work Contract

This review records the authority-closure publication state only. No runtime, broker, order, feed, risk, dashboard, strategy-threshold, replay, WFA, or holdout path is changed.

## Scope Guard

The published source census remains valid as `PROVISIONAL_CENSUS_WITH_DECLARED_GAPS`. Only the authority-closure layer is invalidated. It must not be used as dataset, signal, strategy, blocker, or priority authority evidence.

## Prior Invalidation History

Commit `76584f3e3a8c659a37583e79c485482ec0e852d2` was invalidated because it fabricated family, version, matrix, and priority records from aggregate counts.

Commit `2d7dfa1eaab7afa9b60e37967cd6236b0edcb152` partially repaired the loader so it reads the real external registries, but it did not complete publication of real authority evidence.

## Grill Me Review

Confirmed remaining defects:

- `closure.py` and the focused pytest hardcode `/Users/madhuram/...` full-run paths, so GitHub runners cannot execute the closure independently.
- The committed authority JSON files and sidecars were not regenerated by the repair commits.
- `dataset_version_authority_decisions.json` still contains the prior fabricated `VERSION:0001`, `VERSION:0002`, and related placeholder records.
- Dataset-family decisions remain shallow and are not joined to the full partition, physical-candidate, exact-blob, provenance, temporal, roll, volume, and quality evidence required by the closure contract.
- Dataset-version decisions mostly mirror the census status instead of issuing traceable authority decisions for each real version.
- The unresolved-source output embeds aggregate/raw records but does not deduplicate the 24 candidates into unique physical sources and materiality decisions.
- The signal-ledger output embeds the source audit but does not re-derive the complete causal and provenance authority decision.
- Strategy rows expose only a status and blocker; they omit implementation, parameter, temporal, dataset, signal, split, instrument, option-data, and multi-asset authority components.
- Prioritization maps blocker classes to numeric ranks; it is not evidence-completeness scoring and does not enforce a maximum of three legitimate P1 lanes.
- The blocker ledger republishes aggregate blocker counts instead of traceable blocker records linked to affected strategies and evidence.
- The `tests` workflow already reports a failed gitleaks job on the exact head; `tests` and `ci` were not green at invalidation time.

## Hermes Review

The family, version, source, signal, strategy, blocker, and priority layers are not yet closed as independent traceable authority contracts. Reading real input registries is necessary but not sufficient; the emitted and committed evidence must preserve real IDs and supporting records.

## GSD Review

The minimum repair is to inject full-run paths through an explicit verifier/build command, regenerate two fresh external closure runs, compare their semantic hashes, replace stale synthetic repository artifacts with compact summaries linked to those full runs, and use portable controlled fixtures in pytest.

## QA / Safety Review

Safety remains fail-closed:

- `research_only=true`
- `read_only=true`
- `broker_api_called=false`
- `is_order_action=false`
- `allowed_for_live_execution=false`

No authority result from the current closure package may authorize strategy execution.

## Acceptance Proof

A replacement is acceptable only when:

- no committed production or pytest code requires a Mac-only absolute path;
- all real family and version IDs are preserved from the full registries;
- all 986 versions are accounted for exactly once;
- the 24 unresolved candidates are deduplicated into unique physical-source dispositions;
- the real signal candidate is re-audited from its full record;
- the matrix contains all 16 real strategy/hypothesis lanes and all required authority components;
- blocker and priority outputs are evidence-derived;
- two fresh full closure runs match semantically;
- compact repository summaries and sidecars are regenerated from those runs;
- focused, option-E2E, gitleaks, Code Excellence, Agent Review, and every exact-head workflow pass.

## Runtime Proof Required After Merge

None. This remains research-only evidence and must not be merged or used for runtime promotion before the authority publication gate is satisfied.

## What This PR Does Not Prove

The current closure does not prove dataset authority, signal authority, strategy authority, authority-complete priorities, profitability, replay correctness, WFA, holdout performance, paper readiness, or live readiness.

## Human Approval

PR #710 must remain draft and unmerged. Human approval is required after a replacement closure passes independent audit and all exact-head workflows.