---
mode: AGENT_REVIEW
candidate_id: QA-CERT-FOUNDATION-V1
decision: NOT_CERTIFIED
reason: FOUNDATION_IMPLEMENTED_CERTIFICATION_BLOCKERS_REMAIN
timestamp: 2026-07-30T19:35:00Z
is_order_action: false
broker_api_called: false
source: github_ci_static_audit_and_evidence_artifacts
---

# Enterprise QA Foundation V1 — Agent Review Evidence

## Agent Work Contract

Objective: establish the first auditable slice of a repository-wide QA certification program without changing production trading behavior.

Allowed scope:

- QA strategy and certification documentation;
- module risk classification and ownership;
- deterministic authentication behavior tests;
- test-integrity, runtime-shim, security, dependency, coverage, and unpatched-owner truth gates;
- correction of false-positive defects in QA/forensics tooling;
- evidence required to explain what has and has not been proven.

Forbidden scope:

- changing authentication or trading behavior merely to satisfy tests;
- weakening safety assertions;
- enabling broker access in CI;
- suppressing a real vulnerability or known compatibility patch;
- claiming repository-wide certification from this foundation PR;
- merging the PR without human review.

## Scope Guard

Changed files are limited to QA documentation, CI workflows, QA audit tools, repo-forensics classifier corrections, and tests. No broker order path, strategy, risk, trade construction, approval, execution, dashboard, persistence, or reconciliation runtime implementation is modified.

The tests must exercise public or operationally meaningful behavior. Tests that only import a module, assert a mock call with no state/output invariant, use unconditional assertions, or remain skipped are not accepted as certification evidence.

## Grill Me Review

Questions used to challenge the work:

1. Does a large test count prove safety? No. Duplicate parameter cases, skipped skeletons, weak mocks, and line-only execution can inflate counts without detecting behavioral defects.
2. Does a green full pytest run prove live correctness? No. Network, broker, market-session, load, timing, restart, and controlled-live behavior require separate evidence lanes.
3. Can 100% line coverage prove branch safety? No. Critical allow/block branches and fail-closed transitions must each be asserted and mutation-tested.
4. Could deterministic auth tests pass while the token is invalid in the real broker? Yes. The suite uses deterministic fakes and proves local contracts, not current broker validity.
5. Could test-only shims hide production defects? Yes. Existing compatibility/shim mechanisms remain explicit certification blockers until owner-module behavior passes without them.
6. Could the audit tools themselves be wrong? Yes. The safety and evidence classifiers are being tested against false-positive controls before their counts are treated as certification truth.

Verdict: limited foundation acceptance only. The additions are suitable to continue the certification program, but they do not certify TradeBot as a whole.

## Hermes Review

Traceability review:

- Enterprise QA principles are documented in `docs/qa/ENTERPRISE_QA_MASTER_PLAN.md`.
- Initial risk ownership is documented in `docs/qa/MODULE_RISK_REGISTER.md`.
- Current fail-closed status is documented in `docs/qa/CERTIFICATION_STATUS.md`.
- Authentication behavior evidence is implemented in `tests/auth/test_auth_manager_behavior_contracts.py`.
- Test-integrity evidence is generated by `scripts/audit_test_integrity.py`.
- Automatic patch evidence is generated by `scripts/audit_runtime_shims.py`.
- Unpatched critical owner behavior is exercised by `.github/workflows/qa-unpatched-owner-truth.yml`.
- Security, dependency, coverage, integrity, shim, and forensics lanes are defined in `.github/workflows/qa-truth-baseline.yml`.
- Repo-forensics classifier accuracy is protected by `tests/test_repo_forensics_safety_boundary.py`.

Evidence gaps are not hidden: repository-wide branch coverage, mutation score, dependency remediation, shim retirement, performance budgets, integration environments, replay/live parity, and controlled-live proof remain certification work.

## GSD Review

Delivery review:

- The PR is isolated and draft.
- Production trading runtime code is untouched.
- The initial deterministic full suite completed with 6,774 passed, 9 skipped, and 28 deselected.
- Test-integrity scanning found legacy skipped placeholders, empty tests, and tests requiring oracle review; no new blocking integrity defect was introduced by this PR.
- Bandit reported no medium/high finding in the auth boundary.
- Dependency audit identified a real Autobahn vulnerability inherited through the Kite client dependency graph.
- Runtime-shim audit identified active automatic import-time patches and correctly failed certification.
- Zero-debt repo forensics failed, but review exposed false positives in the safety and evidence classifiers; those tools are being corrected with negative controls rather than trusted blindly.

No failed assertion, vulnerability, active shim, or unknown has been waived. Any failure must be classified as a test defect, audit-tool defect, environment defect, product contract defect, or real residual risk with evidence.

## QA / Safety Review

Safety invariants for this PR:

- no test may contact Zerodha or any broker endpoint;
- no environment token is accepted unless the explicit CI allow flag is enabled;
- canonical file-token precedence is asserted;
- missing mandatory credentials fail;
- authentication errors invalidate cached trust;
- network ambiguity remains distinct from verified authentication;
- persisted authentication state tolerates malformed files without crashing;
- logs may expose only credential fingerprints/tails, never full secrets;
- tests and scanner fixtures cannot be misreported as production broker call paths;
- an actual paper/sim import of direct broker capability or order call remains blocking;
- certification remains red while automatic TradeBot behavior patches are active.

A critical finding remains recorded for subsequent hardening: `validate_token()` can currently return `ok=True` with `auth_state="UNKNOWN_NETWORK"`. Consumers must not treat the boolean alone as proof of authenticated readiness. This PR tests and documents that current contract; it does not silently change it.

## Acceptance Proof

Acceptance evidence required for this foundation PR:

1. `pytest -q tests/auth/test_auth_manager_behavior_contracts.py`;
2. full deterministic pytest lane in paper/offline mode;
3. test-integrity auditor and its self-tests green;
4. auth Bandit scan green at medium/high severity;
5. review evidence validator green;
6. repo-forensics and Code Excellence classifier regression tests green;
7. no secrets present in committed tests or logs;
8. every intentional certification blocker remains visible and red.

A green foundation PR does not require hiding known certification blockers. It requires proving that the program detects and records them honestly.

## Runtime Proof Required After Merge

This PR should not be merged solely because deterministic tests pass. Before final TradeBot QA certification, separate evidence is required for:

- real token expiry and refresh lifecycle in a controlled non-trading session;
- broker-profile timeout and invalid-session differentiation;
- websocket auth failure propagation into feed and candidate readiness;
- no order placement while auth is unverified;
- restart recovery from persisted `AUTH_REQUIRED` state;
- log inspection proving full credentials are absent;
- extended feed and execution safety soak tests;
- full critical-owner suite without `sitecustomize.py` behavior replacement;
- dependency remediation and broker/WebSocket compatibility proof;
- mutation-quality evidence for Tier A modules.

These are certification-program requirements, not proof supplied by this foundation PR.

## What This PR Does Not Prove

This PR does not prove:

- repository-wide QA certification;
- trading profitability or strategy edge;
- correct live broker behavior;
- full code or branch coverage;
- mutation-test strength;
- absence of all security vulnerabilities;
- performance, load, stress, endurance, or penetration readiness;
- replay/live parity;
- execution exactly-once behavior;
- reconciliation correctness;
- dashboard truth;
- that all existing tests are strong;
- that compatibility-shim behavior matches owner-module runtime behavior.

## Human Approval

Human approval is required before merge. The reviewer must confirm that the limited scope and non-claims are understood, that no trading runtime behavior changed, that audit-tool corrections preserve real detections, and that all expected foundation checks on the final head are green. Approval of this PR is approval to continue the QA certification program, not certification of the entire TradeBot.
