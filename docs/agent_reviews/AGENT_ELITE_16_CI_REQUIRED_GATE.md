# AGENT-ELITE-16 — Agent Elite CI Required Gate

mode: REVIEW
candidate_id: AGENT-ELITE-16-CI-REQUIRED-GATE
decision: review_pending
reason: agent_elite_ci_required_gate
source: docs/agent_reviews/AGENT_ELITE_16_CI_REQUIRED_GATE.md
timestamp: 2026-05-28T21:05:00Z
is_order_action: false
broker_api_called: false
live_order_action: false
broker_order_action: false

Issue: #388
Parent: #372
Depends on: #387 / PR #403 / merge commit c649fab1f29d6020511a77219a8525a780e68cf6

## Agent Work Contract

This PR implements AGENT-ELITE-16 only.

The work wires the unified Agent Elite report into CI as a required gate so future PRs cannot bypass runtime/test/safety/evidence truth reporting.

It must not change scanner behavior, run live runtime, call brokers, modify broker code, change dashboard/UI behavior, or change strategy behavior.

## Scope Guard

Allowed:

- Update `.github/workflows/code-excellence-gates.yml` to require the Agent Elite report step.
- Update `scripts/run_agent_elite_report.py` with required-gate exit behavior.
- Extend `tests/test_code_excellence_ci_wiring.py` for missing report, hard failure, unknown explanation, warning, and clean pass behavior.
- Add this agent-review evidence file.

Not allowed:

- Scanner behavior changes.
- Runtime execution.
- Broker calls.
- Broker code changes.
- Dashboard/UI changes.
- Strategy behavior changes.
- External agent calls.
- Hidden blocker bypass.

## High-Risk Path Review

This PR adds CI enforcement only.

High-risk Tradebot paths intentionally unchanged:

- `core/kite_client.py`
- `core/execution_engine.py`
- `core/execution_router.py`
- `core/risk_engine.py`
- `core/orchestrator.py`
- `strategies/`
- `dashboard/`
- `config/`

## Grill Me Review

Question: Does this add scanner behavior?

Answer: No. It only enforces the report generated by the existing Agent Elite aggregation path.

Question: Does this hide report failures?

Answer: No. The workflow no longer bypasses the Agent Elite report step with a shell success fallback.

Question: What happens if the report is missing?

Answer: The required gate fails.

Question: What happens if the unified report says `FAIL`?

Answer: The required gate fails.

Question: What happens if the unified report says `UNKNOWN`?

Answer: The required gate requires a non-empty explanation file.

Question: What happens if the unified report says `PASS_WITH_WARNINGS`?

Answer: The required gate passes while preserving warning visibility in the report artifact.

Question: What happens if the unified report says `PASS`?

Answer: The required gate passes.

## Hermes Review

The implementation is intentionally minimal:

- Adds `--require-ci-pass` to `scripts/run_agent_elite_report.py`.
- Adds `--unknown-explanation` to require an explanation for unknown verdicts.
- Makes the workflow Agent Elite report step required.
- Keeps artifact upload under `if: always()`.
- Keeps the existing unified CE gate command intact.

## GSD Review

Smallest safe implementation:

- No production paths touched.
- No scanner behavior changed.
- No new runtime path.
- CI now requires the report file and verdict policy.
- Existing CE report artifact still uploads for debugging.

Files changed:

- `.github/workflows/code-excellence-gates.yml`
- `scripts/run_agent_elite_report.py`
- `tests/test_code_excellence_ci_wiring.py`
- `docs/agent_reviews/AGENT_ELITE_16_CI_REQUIRED_GATE.md`

## QA / Safety Review

Focused command:

```bash
PYTHONPATH=. pytest tests/test_code_excellence_ci_wiring.py -q
```

Safety assertions:

- No production code touched.
- No runtime import of target Tradebot modules.
- No broker calls.
- No order behavior.
- No live execution.
- No dashboard behavior change.
- Broker code is untouched.
- Strategy/ranking formulas are untouched.
- No code mutation path exists.
- No report bypass is left in the workflow.

## Acceptance Proof

The tests prove:

- Missing report fails.
- Hard failure verdict fails.
- Unknown verdict requires explanation.
- Unknown with explanation passes.
- Warning-only and clean pass verdicts pass.
- Workflow requires `--require-ci-pass`.
- Workflow uploads the unified Agent Elite report artifact.

## Runtime Proof Required After Merge

No live runtime proof is required for this PR. This is CI/report enforcement only.

## What This PR Does Not Prove

- Does not prove live startup succeeds.
- Does not prove candidate quality.
- Does not prove ranking formula quality.
- Does not prove broker readiness.
- Does not prove profitability.
- Does not prove dashboard accuracy.
- Does not validate dynamic runtime dispatch.

## Human Approval

Required before merge.
