# V33C source-volume survivability

The canonical runtime imports modules and invokes scripts lazily from the successor checkout on `/Volumes/TradeBotData`; source-volume disappearance therefore remains `UNKNOWN` for unrestricted continuation. The conservative selected model is `EMERGENCY_CONTINUATION_DISABLED_SHUTDOWN_ONLY`. This resolves the safety decision without pretending the process can continue safely after source loss.
