## Tradebot Agent Work Contract

source_agent: GSD
action: IMPLEMENT_WFA
title: Walk-Forward Analysis Framework
scope: Add WFA optimization and testing over rolling multi-year periods with strict 0.2% slippage.
requested_paths: core/backtesting/wfa.py, scripts/run_wfa.py
allowed_paths: core/backtest_elite.py, core/backtesting/wfa.py, scripts/run_wfa.py, tests/test_wfa.py, core/vectorized_signals.py
forbidden_paths: main.py, run_live.sh, core/execution*, core/broker*, core/risk*
expected_tests: tests/test_wfa.py
acceptance_proof: Run scripts/run_wfa.py to demonstrate the OOS equity curve output and parameter stepping.

### What changed?
- Added `WalkForwardAnalyzer` in `core/backtesting/wfa.py` to prevent curve-fitting via rolling train/test splits.
- Introduced `scripts/run_wfa.py` which executes WFA and imposes a strict 0.2% (20 bps) slippage penalty on every fill.
- Fixed a boolean masking bug in `core/vectorized_signals.py` where dropped invalid signals misaligned the strategy array sizes.
- Added comprehensive unit tests in `tests/test_wfa.py` to assert temporal isolation.

### Why does this move safety/stability/readiness forward?
By enforcing an out-of-sample Walk-Forward Analysis loop with severe simulated execution friction (0.2% slippage), we ensure that the backtesting results correspond much more realistically to actual forward performance rather than in-sample curve fitting.

### What did not change?
Live execution, broker API hooks, routing, or the core architecture in `main.py` were entirely untouched.

### What tests prove it?
`tests/test_wfa.py` directly validates WFA windows to guarantee zero data leakage between training and testing splits.

### What could still fail?
The backtest engine currently skips loading external synthetic chain data for optimization performance. If option chain logic is required downstream, it may differ from actual options constraints.
