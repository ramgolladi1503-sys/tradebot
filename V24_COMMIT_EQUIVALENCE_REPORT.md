# V24 Commit Equivalence Report

This record is populated after the freeze commit and compares the committed
source bytes and tree scope with the precommit validated state.

```text
COMMIT_BYTE_EQUIVALENCE_PASS=PENDING
COMMIT_TREE_SCOPE_PASS=PENDING
```

