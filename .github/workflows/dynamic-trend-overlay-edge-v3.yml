name: Dynamic Trend Overlay Edge V3

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "V3 is retained for manual evidence reproduction; V4 is the final PR campaign."
