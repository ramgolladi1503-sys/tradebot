name: Observe Upstox V3 Depth Capture V1

on:
  push:
    branches:
      - ops/observe-liquidity-exhaustion-depth-readiness-v2
    paths:
      - ".github/workflows/observe_upstox_v3_depth_capture_v1.yml"
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  observe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ops/observe-liquidity-exhaustion-depth-readiness-v2
          fetch-depth: 1

      - name: Query latest capture-contract run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          gh api \
            "/repos/${GITHUB_REPOSITORY}/actions/workflows/upstox_v3_depth_capture_contract.yml/runs?branch=fix%2Fupstox-v3-depth-capture-v1&event=push&per_page=10" \
            > /tmp/runs.json
          python - <<'PY'
          import json
          from pathlib import Path
          payload = json.loads(Path('/tmp/runs.json').read_text())
          runs = payload.get('workflow_runs', [])
          result = {'status': 'NOT_FOUND'}
          if runs:
              run = runs[0]
              result = {
                  'workflow_name': run.get('name'),
                  'workflow_run_id': run.get('id'),
                  'run_number': run.get('run_number'),
                  'status': run.get('status'),
                  'conclusion': run.get('conclusion'),
                  'head_sha': run.get('head_sha'),
                  'created_at': run.get('created_at'),
                  'updated_at': run.get('updated_at'),
                  'html_url': run.get('html_url'),
              }
          target = Path('docs/ops/upstox_v3_depth_capture_v1_status.json')
          target.parent.mkdir(parents=True, exist_ok=True)
          target.write_text(json.dumps(result, indent=2, sort_keys=True) + '\n')
          print(json.dumps(result, indent=2, sort_keys=True))
          PY

      - name: Commit observer result
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/ops/upstox_v3_depth_capture_v1_status.json
          git commit -m "ops: publish Upstox V3 depth capture status"
          git push origin HEAD:ops/observe-liquidity-exhaustion-depth-readiness-v2
