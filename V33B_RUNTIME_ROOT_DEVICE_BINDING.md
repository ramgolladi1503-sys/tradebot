# V33B runtime binding

The governed runtime root must resolve below `/Volumes/TradeBotData` and share its startup-captured `st_dev`. The current host probe is APFS device `16777240`; this is evidence only, not a permanent configuration constant. Runtime/log/DB/desk/report/lock paths are bound beneath the output root by the launcher environment.
