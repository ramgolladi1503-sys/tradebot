# V38 startup order

Candidate SHA → release image → external storage/device → internal emergency
storage → disk reserve → contract hashes → credential authority → same-day
instrument authority → read-only broker auth → feed connect → subscriptions →
persistence → canonical cycle → CAS armed → prospective observation.

The order is fail-closed and frozen for the future session.
